/ {
    chosen {
        zmk,display = &nice_view;
    };
};

/*
 * Override the SPI CS pin used by the nice!view shield.
 * nice!nano v2: D1 = P0.06 = &pro_micro 6
 */
&nice_view_spi {
    cs-gpios = <&pro_micro 6 GPIO_ACTIVE_HIGH>;
};
